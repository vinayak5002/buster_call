#include<stdio.h>
int main(){
    int x,y,i;
    scanf("%d %d",x,y);
for(i=0;i<1000;i++)
    if(x!=0 || y!=0){
        if(x<m)
            printf("%d %d",x,m);
        else
            printf("%d %d",y,x);
    }else
        break;
    }
    return 0;
}
