#include<iostream>
#include<cstdio>
#include<cmath>
using namespace std;

int main(){
    int a,b,C;
    double S,L,h;
    cin >> a >> b >> c;
    
    double rad = C/180*M_PI; 
    
    S = a*bsin(rad)/2.0;
    L = a + b + sqrt(a*a + b*b - 2*a*b*cos(rad));
    h = 
    
    
    
}
