#include<bits/stdc++.h>
using namespace std;
int main(){
    int a;
    cin>>a;
    for (int i=0;i<=a;i++){
        cin<<b;
        int max,min,sum;
        max,min,sum=0
        if(a>max){
            max=a;
        }
        if(a<min){
            min=a;
        }
        sum=sum+a;
    }
    cout<<min<<max<<sum<<endl;
}
