#include<iostream>
#include<stdio.h>

using namespace std;
/*a<b<cにしたいんよ*/
/* 入れ替え3手順で要素３の並び替えってできるんね。これ高校とかでやったっけ？並び替えは数aなかったきがする。地頭問題？*/

int main(){
    int a,b,c,b;
    cin >> a >> b >> c;
    if (a>b);{
        a = d;
        b = a;
        d = b;
    }else if (b>c);{
        c = d;
        b = c;
        d = b;
    }else if (a>b){
        a = d;
        b = a;
        d = b;
    }cout << a <<" "<< b <<" "<< c << endl;
    return 0;
}
