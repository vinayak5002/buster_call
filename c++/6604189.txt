#include<stdio.h>
int main(){
    int i,x;
    do{
        scanf("%d\n",&x);
        i=1;
        i++;
        printf("Case %d: %d",i,x);
        
            
        
    }while(i<=10000 && x<=0);
    return 0;
}
