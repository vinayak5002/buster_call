#include <stdio.h>

int main(int argc,const char * argv[]){
    int a=0,b=0;
    scanf("%d %d", &a, &b);
    lap=a + b;
    area=a * b;
    prinrf("lap\n", lap);
    printf("area\n", area);
    return 0;
    
}
