#include <bits/stdc++.h>
using namespace std;

int main() {
    vector <vector <vector<int> > > state(4, vector< vector<int> >(3, vector<int>(10)));
    
    int n;
    cin >> n;
    for(int i=0;i<n:i++) {
        int b,f,r,v;
        cin >> b >> f >> r >> v;
        b -= 1
        f -= 1
        r -= 1
    }
}
