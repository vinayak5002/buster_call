#include <iostream>
using namespace std;
 
int main(){
  int a,b,c,N;
  cin >> a >> >> b >> c;
  for(int i = a; i <= b; i++){
       if(c % i == 0){
           N++;
       }
    }
  cout << n << endl;
  return 0;
}
