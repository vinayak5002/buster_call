#include<studio.h>
int main(void){
    int a,b;
    char op;//文字列
    while(1){//実行する処理
        scanf("%d %c %d",&a,&op,&b);
        if(op==?)break;
        else if(op==+);
        else if(op==-);
        else if(op==*);
        else if(op==/);
    }
}
