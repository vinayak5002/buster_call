#include<stdio.h>

int main()
{
    int W, H, x, y, r ;
    scanf( "%d %d %d %d %d", W, H, x, y, r ) ;
    if( (y - r) >= 0 && (y + r) <= H && (x - r) >= 0 && (x + r) <= W ) 
    {
        printf( "Yes\n" ) ;
    }
    else
    {
        printf( "No\n" ) ;
    }
    
}
