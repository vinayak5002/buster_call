#include<stdio.h>
int main(){
	int n;
	int i;
	int a[i];
	int MAX=-1000000,MIN=1000000;
	int SUM=0;
	scanf("%d",&n);
	for(i=0;i<n;i++){
		scanf("%d",&a[i]);
		if(MAX<=a[i]){
			MAX=a[i];	
		}
		if(MIN>=a[i]){
			MIN=a[i];
		}
		SUM=SUM+a[i];
	}
	printf("%d %d %d\n",MIN,MAX,SUM);
	return 0;
}
