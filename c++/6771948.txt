#include <stdio.h>

#define MAX 100+1

insertionSort(A, N){ // N個の要素を含む0-オリジンの配列A
    int i, j, v;
    
    for(i = 1; i < N - 1; i++){
        v = A[i];
        
        j = i - 1;
        while(j >= 0 && A[j] > v){
            A[j + 1] = A[j];
            j--;
        }
        
        A[j + 1] = v;
        
        for(i = 0; i < N; i++){
            printf("%d ", a[i]);
        }
        printf("\n");
    }

}

int main(void){
    int N;
    int A[MAX];
    
    scanf("%d", &N);
    
    for(i = 0; i < N; i++){
        scanf("%d", &a[i]);
    }
    
    return 0;
}
