#include <stdlib.h>
#include <time.h>
int main(void)
{
    int a;
    int b;
    if (a > b) {
        printf("%d > %d\n", a, b);
    }
    else if(a == b){
        printf("%d == %d",a,b)
    }
    else(a < b) {
        printf("%d < %d\n", a, b);
    }
    return 0;
}

