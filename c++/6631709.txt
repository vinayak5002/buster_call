#include<bits/stdc++.h>
using namespace std;

int main(){
    double p = 3.141592;
    double r;
    cin >> r;
    cout << fixed << setprecision(6) << p*r*r << " " << fixed << setprecision(6) << 2*p*r << endl;
}
