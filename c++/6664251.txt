#include <bits/stdc++.h>
using namespace std;

int main(void)
{
  double r;
  cin >> r;
  double p = 3.1415926535;
  cout << fixed << setprecision(10) << r * r * p << " " << 2 * r * p << endl;
  return 0;
}
