#include<bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef long double ld;

int32_t main()
{
    ll a, b, c;
    cin >> a >> b >> c;
    
    cout << min({a, b, c}) << endl;
    
    cout << max({a, b, c}) << endl;
    
    return 0;
}
