#include <stdio.h>
int main() {
    int i, j;
    for(i = 1; ; i++) {
        scanf("%d\n", &j)
        if(j == 0)
            break;
        printf("Case %d : %d\n", i, j);
        }
    return 0;
    }

