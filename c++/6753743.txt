#include <bits/stdc++.h>
using namespace std;
int main(){
    while (true){
        int num;
        cin >> num;
        if (num == 0) break;
        if (num < 0) continue;
        cout << num << endl;
    }
    return 0;
}
