#include<iostream>
#include<cstdio>
#include<cctype>
using namespace std;

int main(){
    char a;
    int cnt[26] = {};
    
    while(cin >> a ){
        a = tolower(a);
        if (isalpha(a)){
    }
}
