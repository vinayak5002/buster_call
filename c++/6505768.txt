#include<stdio.h>

int main(){
    char num[x];
    char total;
    int x;
    
    while(1){
        scanf("%s",num);
        
        if(num == 0) break;
        for(i = 0;i <x; i++){
            total += num[i]
        }
    }
}
