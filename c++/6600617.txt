#include<iostream>
using namespace std;

int main(){
    int x,y;
    cin>>x,y;
    for(int i=1;;i++)
    if (x == 0 && y == 0) break;
    
    if (x < y) ;
    cout << x << " " << y << endl;
    else if else  (x > y) ;
    cout << y << " " << x << endl;
}
