#include<iostream>
using namespace std;

int main(){
    int x,y;
    cin >> x >> y;
    for (int i=1;;i++)
    if (x == 0 && y == 0) break;
    cout if (x > y) << y << " " << x <<endl;
    else << x << " " << y << endl;
}
