#include<iostream>
using namespace std;
int main(){
    int w,h,x,y,r;
    cin>>w>>h>>x>>y>>r;
    if(x>=r && x+r<=w && y>=r && y+r<=h ){
        cout<<"Yes"<<endl;
    }
    else cout<<"No"<<endl;
    
}
