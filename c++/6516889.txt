#include<stdio.h>

int main(void)
{
    int n; //プロセス数
    int q; //クォンタム
    
    char *nArray = (char *)malloc(sizeof(char)*10*n);
    int *tArray = (int *)malloc(sizeof(int)*n); //プロセス数分動的割り当て
    
    for(int i=0; i<n; i++){
        scanf("%S%d", &pArray[i], &tArray[i]);
    }
    
    
}
