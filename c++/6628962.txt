#include<iostream>
#include<cctype>
using namespace std;

int main(){
    char ch;
    for(int i=0;i<1200;i++){
        cin >> ch;
        if(int isalpha(ch)) if(int islower(ch))int toupper(ch)
    }
}

