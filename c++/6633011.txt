#include<bits/stdc++.h>
using namespace std;

int main(){
    double a, b;
    cin >> a >> b;

    double area, length;
    area = a * b;
    length = (a + b) * 2;

    cout << area << " " << length << endl;
}
