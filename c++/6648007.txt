#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b;
    
    scanf("%d %d",&a, &b);
    if(a > b) printf("a > d");
    else if(a < b) printf("a < d");
    else printf("a == d");
    return 0;
}
