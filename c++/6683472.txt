#include<iosream>
using namespace std;
int main(){
    int n,i;
    int v,j;
    for(i=0;i<n;i++){
        v = A[i];
        j = i - 1;
        while j >= 0 && A[j] > v{
            A[j+1] = A[j]
            j--;
        }
        A[j+1] = v;
    return 0;
    }
}
