#include<bits/stdc++.h>

using namespace std;

typedef long long ll;

int32_t main()
{
    int a, b; 
    cin >> a >> b;
    
    cout << a/b << " " << a%b << " " << fixed << setprecision(5) << (double)a/b << endl;
    
    return 0;
}
