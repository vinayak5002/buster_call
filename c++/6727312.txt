#include <bits/stdc++.h>
using namespace std;
using ll = long long;



int main(){
    cin.tie(nullptr);
    ios::sync_with_stdio(false);
    int s, t, y;
    cin >> s >> t >> y >> y >> y >> y;
    cout << (s ^ t ^ y) << endl;
}
