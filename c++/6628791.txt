#include<iostream>
#include<cctype>
using namespace std;
int main(){
    char a = 'A';
    cin >> a;
    if(int isdigit(char a)){
        int toupper(char a);
    }
    else if(int isupper(char ch)){
        int toupper(char a);
    }
    cout << a << endl;
    return 0;
}
