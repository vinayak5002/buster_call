#include <bits/stdc++.h>
using namespace std;

int main() {
    int N,M;
    cin >> N >> M;
    vector<int> A(N);
    vector<int> B(M);
    
    int cnt;
    for(int i=0;i<=100;i++) {
        if(find(A.begin(). A.end(), i) && find(B.begin(). B.end(), i)) cnt += 1;
    }
    cout << cnt << endl;
}
