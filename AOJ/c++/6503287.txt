#include <stdio.h>
int main(void){
    int a, b, c;
    printf("46979：");
    scanf("%d", &sec);
    
    c = a / 3600;
    b = (b%3600) / 60;
    b = b % 60;

    printf("%d:%d:%d\n", a,b,c);
    return 0;
}
