#include <stdio.h>
#include <iostream>
using namespace std;

int main(){
    int W, H, x, y, r;
    cin >> W >> H >> x >> y >> r;
    if(x>=r && y>=r && w-x>=r && h-y>=r) cout << "Yes" << endl;
    else cout << "No" << endl;
}

