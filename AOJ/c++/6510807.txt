#include <stdio.h>
int main(int argc, const char * argv[]) {
    // insert code here...
    int x; //変数の宣言
    while (1) {
    scanf("%d", &x);//xに値を読み込む
    printf("Case i: %d", x);
    if (x == 0)break;
    }
   
    
    return 0; }
   
