#include <stdio.h>

int main()
{
    int i, n;
    for(i = 0; i<n; i++)
        scanf("%d", &n);
        int a[n];
    for(i = 0; i<n; i++){
    scanf("%d", &a[i]);
    }
    for(i=n-1; 1>=1; i--){
        printf("&d ", a[i]);
    }
    printf("%d\n", a[1]);
    return 0;
    
}
