#include <bits/stdc++.h>
using namespace std;
// using ll = long long;
// #define xfor(i, b, e) for (int i = (int)(b); i < (int)(e); ++i)
// #define xrep(i, n) xfor(i, 0, n)
int main() {
    int r;
    cin >> r;
    cout << setprecision(8) << M_PI*r*r << ' ' << 2*M_PI*r;

    return 0;
}
