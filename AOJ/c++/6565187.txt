#include<iostream>

int main() {
    int a, b, c;
    std::cin >> a >> b >> c;
    int count = 0;
    
    for (int i = a; i <= b; i++) {
        if (c % i == 0) {
            count++;
        }
    }
    
    std::cout << count << "\n";
    return 0;
}
