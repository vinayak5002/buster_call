#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    
    int N,i;
    int count = 1;
    char S[5] = {A,B,C,D.E};
    
    srand(time(NULL));
    i = rand() % 4;
    
    while(count > N) {
        printf("%c\n",s[i]);
        count++;
    }

    return 0;
}
