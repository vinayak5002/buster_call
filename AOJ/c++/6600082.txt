#include<iostream>
using namespace std;
int main(){
    int x;
    int x[n];
    cin >> x;
    for ( int i = 0; i < n; i++ ){
    if ( i ) cout << " "; // i が1以上のとき、つまり最初の要素ではないとき、空白を出力 
    cout << x[n-i] << " " ;
    }
cout << x[0] << endl;
}
