#include <iostream>
#include <cmath>
#include <algorithm>
#include <climits>
#include <iomanip>
#define _USE_MATH_DEFINES
using namespace std; 

int main()
{
	int n;
	cin>>n>>endl;
	int minX=INT_MAX;
	int maX=INT_MAX+1;
	for (int i=0; i<n; i++)
	{
		int x;
		cin>>x;
		minX=min(minX,x);
		int y;
		cin>>y;
		maX=max(maX,y)
		sum==x+y
	cout<<minX<<" "<<maX<<" "<<sum
		
	}
}

