#include <bits/stdc++.h>
using namespace std;

int main(){
    int x, y;
    //float z;
    
    cin >> x >> y;
    //z = x % y;
    
    cout << x / y << " "<< int(x % y) << " " << float (x % y) << endl;
}
