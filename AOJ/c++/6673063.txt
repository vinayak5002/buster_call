#include<iostream>
using namespace std;

int main() {
    string mark[2] = {"#", "."};
    
    while(true) {
        int H,W;
        cin >> H >> W;
        if(H==0 && W==0) break;

        for(int i=0;i<H;i++) {
            string line;
            int flag = 0 if(i%2==0) else 1;
        }
    }
}
