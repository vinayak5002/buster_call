#include <stdio.h>

int main(void) {

    int i, cnt;
    int a, b, c;

    cnt = 0;
    scanf("%d %d %d", &a, &b, &c);

    for (i = a; i <= b; i++) {
        if (0 == c % i) {
            cnt++;
        }
    }

    printf("%d\n", cnt);

    return 0;
}

