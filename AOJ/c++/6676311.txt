#include <iostream>
using namespace std;
int main(void){
    // Your code here!
    int n,s,b,p=0;
    vector<int> vec;
    for (int i=0; i < n; i++) {
        cin >> vec >> endl;
    }
    for (int i=0; i < n; i++) {
        if (a.at(i-1) < a.at(i)) {
            s = vec.at(i);
        }
    }
    for (int i=0; i < n; i++) {
        if (a.at(i-1) > a.at(i)) {
            b = vec.at(i);
        }
    }
    for (int i=0; i < n; i++) {
        p =+ vec.at(i);
    }
}
