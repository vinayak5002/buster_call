#include <bits/stdc++.h>
using namespace std;
// using ll = long long;
// #define xfor(i, b, e) for (int i = (int)(b); i < (int)(e); ++i)
// #define xrep(i, n) xfor(i, 0, n)
int main() {

    int t, j = 1;
    cin >> t;
    while (t) {
        cout << "Case " << j << ": " << t << endl;
        j++;
        cin >> t;
    }

    return 0;
}
