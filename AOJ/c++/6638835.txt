#include <stdio.h>

int main(int argc, const char * argv[]){
    int a=0;
    int b=0;
    printf("a b?");
    scanf("%d %d", &a &b);
    printf("a=%d b=%d\n", a, b);
    printf("perimeter=%d area=%d \n", 2*a+2*b ,a * b));
    return 0;
}
