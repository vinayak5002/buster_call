#include <bits/stdc++.h>
using namespace std;

int main() {
    int x, y;
    cin >> x >> y;
    
    int a, b;
    double c;
    a = x / y;
    b = x % y;
    c = i.0 * x / y;
    
    cout << a << " " << b << " " << c << endl;
}
