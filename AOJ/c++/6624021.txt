#include <stdio.h>
#include <math.h>
int main(void){
    int n,i;
    double s,sum=0,avg;
    
    scanf("%d",&n);
    for(i=0;i<n,i++){
        scanf("%d",&s);
        sum+=s;
    }
    m=sum/n;
    sum=0;
    for(i=0;i<n;i++){
        a+=(s[i]-m)*(s[i]-m);
        
    }
    a/=(double)n;
    a=sqrt(a);
    printf("%lf %lf %lf\n",h*a/2,a+b+d,h);
}
