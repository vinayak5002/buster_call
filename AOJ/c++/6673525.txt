#include<iostream>
#include<stdio.h>
#include<map>
using namespace std;

int main() {
    int n;
    cin >> n;
    map<string, [13]int]> a;
    for(int i=0;i<n;i++) {
        string mark;
        string num;
        cin >> mark >> num;
        a[pair][num-1] += 1;
    }
}
