#include <bits/stdc++.h>
using namespace std;

int nCr(n,r) {
    return;
}

int main() {
    while(true) {
        int n,x;
        cin >> n << x;
        if(n==0 && x==0) break;
        
        pass
    }
}
