#include <stdio.h>
int main(void){
    int x, n;
    x <= 10000;
    n=1;
    while(10000){
    scanf("%d %d", &x, &n); 
    if ( x == 0 ) break;
    if ( x == 10000 ) break;
    printf (" Case %d:%d\n",n,x);
    n+ 1;
}  
}
