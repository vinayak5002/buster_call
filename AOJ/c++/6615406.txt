#include <bits/stdc++.h>
using namespace std;

int main() {
    double r;
    cin >> r;

    double menseki = r * r * M_PI;
    double syuu = 2 * r * M_PI;

    cout << fixed << menseki << ' ' << fixed << syuu << endl;
}
