#include<iostream>
#include<stdio.h>
#include<map>
using namespace std;

int main() {
    int n;
    cin >> n;
    map<string, int> a;
    for(int i=0;i<n;i++) {
        string mark;
        int num;
        cin >> mark >> num;
        string pair = scanf("%s %d"), &mark, &num)
        map[pair] = 1;
    }
}
