#include <bits/stdc++.h>
using namespace std;
// using ll = long long;
// #define xfor(i, b, e) for (int i = (int)(b); i < (int)(e); ++i)
// #define xrep(i, n) xfor(i, 0, n)
int main() {

    int t = 1000;
    while (t--) {
        cout << "Hello World" << endl;
    }

    return 0;
}
