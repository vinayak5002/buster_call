#include<iostream>
#include<cstdio>
#include<cmath>
using namespace std;
int main(){
    int x1,y1,x2,y2 deg;
    double fabs(double X = x1 - x2);
    double fabs(double Y = y1 - y2);
    double sprt(double X + Y);
    double s;
    cin>>x>>deg;
    s = sqrt(X + Y);
    printf(s);
    return 0;
}
