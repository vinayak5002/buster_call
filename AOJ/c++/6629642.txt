#include<iostream>
#include<string>
using namespace std;

int main()
{
	string str;
	getline(cin, str);
	int sum = 0;
	if (str == '0') break;
	
	for (int i = 0; i < str.length(); i++){
	    int num = atoi(Str.c_str());
	    int sum += num
	    
	}
	
	cout << sum << endl;
	
	return 0;
}

