#include<stdio.h>
#include<iostream>

using namespace std;

/*比べる本人より大きいものが右、小さいものが左にいたらいいね。*/
int main(){
    int N;
    cin >> N >> A[];
    for(A[]==0){
        comp = int A[n];
    if (comp<A[n+1],comp>A[n-1]){
        stay ;
    }if else (comp<A[n+1],comp>none){
        stay ;
    }if else (comp>A[n-1],comp<none){
        stay ;
    }else go one step right;
    }
    /*stay したら次の数字に行きたいよね*/
    
}
