#include<iostream>
#include<vector>
using namespace std;

int main() {
    int a,b,c
    cin >> a >> b >> c;
    vector<int> nums = {a,b,c};
    
    sort(vector.begin(), vector.end())
    cout << vector << endl;
}
