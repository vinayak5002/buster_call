#include<cstdio>
#include<cmath>
int main(){
  int a,b,deg;
  double rad;
  
  double PI = acos(-1);  // PI = 3.141593...
  scanf("%lf %lf %lf",&a,&b,&deg);
  rad = deg * PI / 180;
  printf("%d degree = %f radian\n", deg, rad);
  printf("cos(%d) = %f, sin(%d) = %f\n", deg, cos(rad), deg, sin(rad));
  
  
  
  printf("%lf %lf %lf",0.5*a*b*sin(rad),a+b+sqrt(pow(x2-x1,2)+pow(y2-y1,2),b*sin((180-deg)*PI/180));
  
  
  
  
}
