#include<bits/stdc++.h>

using namespace std;

typedef long long ll;

const double pi=3.1416;

int32_t main()
{
    int r;
    cin >> r; 
    
    cout << fixed << setprecision(6) << (double)(pi*(r*r)) << " " << (double)(2*pi*r) << endl;
    return 0;
}
