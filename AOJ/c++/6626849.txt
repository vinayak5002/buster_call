#include <stdio.h>

int h1(int key)
{
	return key%M;
}

int h2(int key)
{
	return 1+(key%(m-1));
}

long getkey(char A[])
{
	long sum=0,i;

int insert(char A[])
{
	long i,n,key;//longはintの拡大版みたいなやつ//
	key=getkey
}

int getchar(char m)//文字を数字に変換//
{
	if(m=='A') return 1;
	else if(m=='T') return 2;
	else if(m=='G') return 3;
	else if(m='C') return 4;
	else return 0;
}


int main(void)
{
int n,i;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	
	
