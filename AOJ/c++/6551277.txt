#include <bits/stdc++.h>

using namespace std;

int main() {
    vector<int> a(10000);
    for (int i = 1 ; i<= 10000; i++) {
        cin >> a[i];
    }
    for (int i = 1; i <= 10000; i++) {
        cout << "Case" + i;
    }
    return 0;
}
