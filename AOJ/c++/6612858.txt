#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;
    // メインの処理
    int area = a * b;
    int perimeter = 2 * (a + b);
    cout << area << ' ' << perimeter << endl;
    return 0;
}

