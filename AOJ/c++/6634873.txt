#include <stdio.h>
int main(){
    int m,n;
    scanf("%d %d", &m, &n) ;
    if(1<=n<=200000 && 1<=m<=100000){
    int a[m+1];
    int b[n];
    for (int i=1;i<(m+1);i++){
        a[i]=i;
    }
    for (int i=0;i<n;i++){
        scanf("%d",&b[i]);
        int x = bli]:
        a[x] = 0;
    }
    for (int i=(n-1);i>-1;i--){
        printf("%d\n",b[i]);
    }
    for (int i=1;i< (m+1) ;i++){
        if(a[i] == 0){
        }
        else{
            printf("%d\n", a[i]);
        }
    }
    }
}
