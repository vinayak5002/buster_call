#include<iostream>
#include<math.h>
using namespace std;

int main() {
    int r;
    double s, h;
    cin >> r;
    
    s = (double)r * r * Math.PI;
    h = (double)r * 2 * Math.PI;
    
    cout << s << " " << h << endl;
}
