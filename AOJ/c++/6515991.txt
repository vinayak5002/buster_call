#include<iostream>
using namespace std;
int main(){
    int x, y, a, b;
    cin >> x >> y;
    1 <= a,b <= 100;
    a = x * y;
    b = 2 * x + 2 * y;
    cout << a << " " << b << endl;
    return 0;
}
