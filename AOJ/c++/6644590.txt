#include <stdio.h>
#include <stdlib.h>
#include <math.h>
 
int main(){
	int i,j,max,arr[100];

    while(scanf("%d", &i)!=EOF) {
       arr[i-1]+1;
       j=max;
       
		  }	
	for(i=0;i<=99;i++){
          if(arr[i]>j){
          	max=arr[i];   
			}
    }
    for(i=0;i<=99;i++){
    	if(arr[i]=j){
    	    printf("%d\n",i);	
		}
	}
}

