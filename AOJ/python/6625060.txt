include <bits/stdc++.h>
using namespace std;

int main() {
    int n, a == 0, i <= n;
    int count = s[i];
    cin >> n >> s >> a;

    if (s[i] == 2 && s[i + 1] == 0 && s[i + 2] == 2 && s[i + 3] == 0) {
       a += 1;
    }
    else if (s = 0) {
              cout << " " << endl;
    } 
    else if (s != 0) {
        cout << a << endl;
    }

}
