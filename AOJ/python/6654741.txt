int main(){
  int a, b, t; // ３つの整数を宣言
  a = 5;       // a に 5 を格納 
  b = 8;       // b に 8 を格納 
  t = a;       // t に a の値を格納 
  a = b;       // a に b の値を格納、この直後 a の値は 8 になる
  b = t;       // b に t の値を格納、この直後 b の値は 5 になる

  return 0;
}
