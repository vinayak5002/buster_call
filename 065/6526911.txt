#include <bits/stdc++.h>
using namespace std;
const double PI = acos(-1);

int main()
{
    double r;
    cin >> r;
    cout << fixed << setprecision(10)
         << PI * r * r << " " << 2 * PI * r;
    return 0;
}
