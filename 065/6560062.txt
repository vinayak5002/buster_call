#include<iostream>
using namespace std;
int main(){
    int n,i;
    int max=1000000,min=1000000,sum=0;
    int an[10000]
    int temp;
    
    cin >> n;
    
    for(i = 0;i < n;i++){
        cout << an[n] << "\n";
        sum = sum + an[n];
    }
    
    max = i[0];
    min = i[0];
    
    for(i = 1;i < n;i++){
        if(max < i[n]){
            max = i[n];
        }
        if(min > i[n]) {
            min = i[n];
        }
    }
    cout << max << " " << min << " " << sum << "\n";
    return 0;
}
