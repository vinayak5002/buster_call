#include <iostream>
#include <iomanip>
#include <algorithim>

int main()
{

    int a, b, c;

    cin >> a >> b >> c;
    
    if (a > b)
        swap(a, b);
    
    if (b > c)
        swap(b, c);
    
    if (a > b){
        swap(a, b) 
    cout << a << " " << b << " " << c << endl;
    }
    


    
