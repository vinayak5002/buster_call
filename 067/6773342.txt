#include <bits/stdc++.h>
using namespace std;
int main(){
    int a,t,r;
    cin >> a >> t >> r;
    cout << fixed << setprecision(12);
    double ans=((double)t/(double)a)*r;
    cout << ans << endl;
}
