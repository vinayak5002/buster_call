#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
using namespace std;

int main() {
    float a, t, r;
    cin >> a >> t >> r;
    cout << (r / a) * t << "\n";
    return(0);
}
