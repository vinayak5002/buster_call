using System:

class a
{
    public static void Main(string[] args)
    {
        int x = Parse(Colsole.ReadLine());
        int y = Parse(Console.ReadLine());
        Console.WriteLine(x*y, (x+y)*2)
    }
}
