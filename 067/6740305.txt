#include <stdio.h>

int main(void)
{
    float a,b;
    printf("a = ");
    scanf("%d", &a);
    printf("b = ");
    scanf("%d", &b);
    if (a == b);
    else if (a > b);
    else ;
    
    return 0;
}
