#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
using namespace std;

int main() {
  float a, t, r, x;
  cin >> a >> t >> r;
  x = (a / t);
  cout << r / x << "\n";
  return(0);
}
