#include <stdio.h>
#include<math.h>

int main (void){
    int n,score[],a,i,sum;
    double ave,sum2,a;
    sum=0;
    printf("学生の数");
    scanf("%d",n);
    for (i=0;i<n;i++){
        scanf("%d",score[i])
        sum=sum+score[i]
        
    }
    ave=sum/n;
    for (i=0;i<n;i++){
        sum2=pow( array[i]-ave,  2)
        
        
        
        
    }
    a=sqrt(sum2/n);
    printf("%f",a);
    
    
    
    
}
