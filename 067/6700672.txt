#include <stdio.h>

int main() {
    int a;
    int b;
    int c;
    int count;

    scanf("%d %d %d", &a, &b, &c);
    
    
    if (c % a = 0) {
        count = 2;
    }
    if (c % b = 0) {
        count = 3;
    }
    printf("%d",count);
    
    return 0;
}
