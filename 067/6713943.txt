#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
using namespace std;

int main() {
    double a, t, r;
    cin >> a >> t >> r;
    printf("%.4f\n", (r / a) * t);
    return(0);
}
