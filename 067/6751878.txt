#include <bits/stdc++.h>
#include <stdio.h>
#define rep(i, n) for (int i = 0; i < (n); ++i)
using namespace std;
int main()
{

    int x, y;
    cin >> x >> y;
    cout << gcd(x, y) << endl;

    return 0;
}
