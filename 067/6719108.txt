#include<bits/stdc++.h>

using namespace std;

int main(){
    int a, b;
    cin >> a >> b;

    int d = a / b;
    int r = a % b;
    double f = a / (double)b;

    printf("%d %d %lf", d, r, f);

    return 0;
}
