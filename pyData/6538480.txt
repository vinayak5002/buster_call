n = int(input())
A_list = [int(input()) for _ in range(n)]

dp = [1] * n

for i in range(n):
    
    for j in range(i):
        
        if A_list[j] < A_list[i]:
            dp[i] = max(dp[i], dp[j] + 1)

print(max(dp))
