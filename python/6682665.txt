while True:
    x,y = map(input().split())
    if x == 0 and y == 0:
        break

    cnt = 0
    for a in range(1,x+1)
       for b in range(1,a+1) 
          for c in range(1,b+1)
             if a + c + b == x:
                  cnt + = 1
     print(cnt)
