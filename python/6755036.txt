using System;

class Program
{
    static void Main()
    {
        string line;
        while ((line = Console.ReadLine()) != null)
        {
            var no = 1;
            Console.WriteLine("Case " + no + ": " + line);
        }
    }
}
