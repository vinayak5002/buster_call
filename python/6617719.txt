S = int(input())

h = S//(60*60)
# 時間になったところの秒数を引く
#  S-=h*3600
S =S - h*3600

m = S//60
# 分になったところの秒数を引く
# S-=m*60
S = S -m*60

s = S
# デフォルトでは半角が入る
# print 関数は引数にsepを指定すると区切り文字を指定できます。

print(h,':',m,':',s, sep='')
