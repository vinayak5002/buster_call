using System;
using System.Linq;

class a
{
   static void Main()
    {
    var S = Console.ReadLine().Split(' ');
    var a = int.Parse(S[0]);
    var b = int.Parse(S[1]);
    
       if( a < b )
        Console.WriteLine("a < b");
        else if( x > y )
         Console.WriteLine("a > b");
        else
            Console.WriteLine( "a == b" );
    }
}

