using System;

class Program {
    static void Main() {
        var i = int.Parse(Console.ReadLine());
        // Console.WriteLine(Math.Pow(input, 3));
        Console.WriteLine(i * i * i);
    }
}

